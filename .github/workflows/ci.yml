name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11', '3.12']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install ruff pip-audit

      - name: Lint with ruff
        run: |
          # Stop the build if there are Python syntax errors or undefined names
          ruff check . --select=E9,F63,F7,F82 --output-format=github
          # Default set of ruff rules with GitHub annotations
          ruff check . --output-format=github || true

      - name: Check for security vulnerabilities in dependencies
        run: |
          pip-audit --strict --desc || true

      - name: Verify package imports correctly
        run: |
          python -c "from claude_monitor.cli import main; print('CLI import OK')"
          python -c "from claude_monitor.web.app import create_app; print('Web app import OK')"

      - name: Verify CLI entry point works
        run: |
          claude-monitor --help

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pip-audit safety

      - name: Run pip-audit
        run: |
          pip install -e .
          pip-audit --strict --desc

      - name: Run safety check
        run: |
          pip install -e .
          safety check || true
